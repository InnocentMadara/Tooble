const lines = Array.from(document.querySelectorAll('.line'));
const ends = Array.from(document.querySelectorAll('.line-end'));

lines.forEach(line => {
  line.style = "width: 100px; height: 100px; background: green"
})